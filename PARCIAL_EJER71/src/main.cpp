#include <Arduino.h>
#include <DataCapture.h>

void tiempo(int, int, int, int);

void setup() {
      int horas, min, seg;
      int totalSeg = DataCapture:: captureInteger ("Digite el numero total de segundos: ");

}

void loop() {
  
}